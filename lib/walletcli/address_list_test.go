package walletcli_test

import (
	"testing"
)

func TestAddrList(t *testing.T) {
	// cli := walletcli.RunMockDaemonService(t)

	// addrlist, err := walletcli.NewAddrList(cli, 30)
	// assert.Nil(t, err)

	// addr := addrlist.Data[0]

	// name, err := addr.AccountName(cli)
	// assert.Nil(t, err)
	// t.Log(name, addr.Amount)

	// assert.NotEmpty(t, addrlist.Index)
	// assert.NotNil(t, addrlist.Index["uqxrxtAhct9Pq7xZRvQ7LcefZXwuo6Ehwg"])

	// t.Run("get big list account", func(t *testing.T) {
	// 	hasil := addrlist.GetBigUnspent(2000)

	// 	assert.NotEmpty(t, hasil)
	// })

}
